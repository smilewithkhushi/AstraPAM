# web
fastapi==0.139.0
uvicorn[standard]==0.51.0
httpx==0.28.1
pydantic==2.13.4
python-dotenv==1.0.1

# ml
torch==2.13.0
scikit-learn==1.9.0
imbalanced-learn==0.14.2
# shap is installed via script.sh with --no-deps (numba doesn't support Python 3.14)
# these are shap's required deps that work fine on 3.14:
slicer==0.0.8
cloudpickle>=1.2.2
tqdm>=4.27.0
pandas==3.0.3
numpy==2.4.6

# ui
streamlit==1.59.2

# dev
ruff==0.15.21

# reporting (phase 10)
fpdf2==2.8.3

# pqc (phase 4)
cryptography==45.0.5
pqcrypto==0.4.0
# liboqs-python is the reference-grade alternative; requires cmake + C compiler
